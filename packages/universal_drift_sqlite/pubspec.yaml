name: universal_drift_sqlite
description: "Reusable Flutter SQLite database layer built with Drift."
version: 0.1.0

environment:
  sdk: ^3.12.0
  flutter: ">=3.24.0"

dependencies:
  drift: ^2.34.0
  flutter:
    sdk: flutter
  path: ^1.9.1
  path_provider: ^2.1.6
  sqlite3: ^3.3.3
dev_dependencies:
  build_runner: ^2.15.0
  drift_dev: ^2.34.0
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  test: ^1.31.0

flutter:
