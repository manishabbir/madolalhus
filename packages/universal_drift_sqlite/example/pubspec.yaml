name: universal_drift_sqlite_example
description: "Example app for universal_drift_sqlite."
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  universal_drift_sqlite:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
